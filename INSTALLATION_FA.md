# راهنمای نصب سرور VPN - فارسی

## مرحله ۱: آماده‌سازی سرور

### نیازمندی‌ها:
- یک سرور Ubuntu (۲۲.۰۴ یا جدیدتر)
- دسترسی SSH به عنوان root یا کاربر با دسترسی sudo
- اتصال اینترنت
- (اختیاری) دامنه ای که به آدرس سرور اشاره کند

### گام ۱: اتصال به سرور
```bash
ssh root@your-server-ip
```
یا اگر کاربر عادی‌تر استفاده می‌کنید:
```bash
ssh your-username@your-server-ip
```

---

## مرحله ۲: دانلود و نصب

### گام ۲: دانلود پروژه
```bash
cd /root
git clone https://github.com/Lucifer-ir/vps.git
cd vps
```

### گام ۳: اجرای اسکریپت نصب

#### اگر دامنه دارید (توصیه‌شده):
```bash
sudo bash scripts/install-server.sh your-domain.com
```
مثال:
```bash
sudo bash scripts/install-server.sh vpn.example.com
```

#### اگر بدون دامنه نصب کنید (از IP سرور استفاده می‌شود):
```bash
sudo bash scripts/install-server.sh
```

---

## مرحله ۳: بعد از نصب موفق

اسکریپت نصب اطلاعات زیر را نمایش می‌دهد:

```
==========================================
Installation Complete!
==========================================
Admin Panel URL: https://your-domain.com/admin
Default Username: admin
Default Password: [رمز عبور تولید شده]
Database: /opt/vpn-server/database/vpn.db

Service Status:
● vpn-server.service - VPN Configuration Server
     Loaded: loaded (/etc/systemd/system/vpn-server.service; enabled; preset: enabled)
     Active: active (running)
...
```

**این اطلاعات را یادداشت کنید!**

---

## مرحله ۴: دسترسی به پنل ادمین

### روش ۱: از طریق SSH Tunnel (امن‌تر)

سرویس فقط برای `localhost` قابل دسترسی است. برای دسترسی از جهت دور، از تونل SSH استفاده کنید:

#### بر روی کامپیوتر شخصی (Windows, Mac, Linux):
```bash
ssh -L 5000:localhost:5000 root@your-server-ip
```

سپس در مرورگر بروید به:
```
http://localhost:5000/admin
```

### روش ۲: کنسول سرور
اگر به صورت مستقیم به کنسول سرور دسترسی دارید:
```bash
# بر روی سرور
firefox http://localhost:5000/admin
```

---

## مرحله ۵: بررسی وضعیت سرویس

### بررسی کنید که سرویس در حال اجرا است:
```bash
sudo systemctl status vpn-server
```

### مشاهده لاگ‌های سرویس:
```bash
sudo journalctl -u vpn-server -f
```

### توقف و شروع مجدد سرویس:
```bash
# توقف
sudo systemctl stop vpn-server

# شروع مجدد
sudo systemctl start vpn-server
```

---

## مرحله ۶: پیکربندی اولیه

### بررسی پایگاه داده:
```bash
sqlite3 /opt/vpn-server/database/vpn.db
.tables
.exit
```

### مشاهده فایل `.env`:
```bash
sudo cat /opt/vpn-server/.env
```

---

## مرحله ۷: راهنمای استفاده

### ورود به پنل ادمین:
1. نام کاربری: `admin`
2. رمز عبور: رمزی که بعد از نصب نمایش داده شد

### قابلیت‌های پنل ادمین:
- ✅ ایجاد و مدیریت تنظیمات VPN
- ✅ مدیریت کاربران برنامه اندروید
- ✅ دانلود APK اپلیکیشن
- ✅ نظارت و مشاهده فعالیت‌های کاربران
- ✅ مشاهده سایت‌ها و اپلیکیشن‌های استفاده‌شده

---

## حل مشکلات رایج

### مشکل: سرویس شروع نمی‌شود
```bash
# بررسی لاگ‌ها
sudo journalctl -u vpn-server -n 100 --no-pager

# اگر مشکل در npm است:
cd /opt/vpn-server
npm install --production
```

### مشکل: نمی‌توانم به `/admin` دسترسی داشته باشم
- اطمینان حاصل کنید که SSH Tunnel اجرا می‌شود
- یا بر روی سرویس مستقیماً: `http://localhost:5000/admin`
- در غیر این صورت، IP شما ممکن است بلاک شده باشد

### مشکل: پایگاه داده خراب است
```bash
# حذف و بازسازی
rm /opt/vpn-server/database/vpn.db
systemctl restart vpn-server
```

---

## دستورات مفید

### بررسی اینکه سرویس به درستی شنوند می‌کند:
```bash
sudo lsof -i :5000
```

### باز کردن port 5000 (اگر فایروال مسدود کند):
```bash
sudo ufw allow 5000/tcp
```

### مشاهده تمام سرویس‌های فعال:
```bash
sudo systemctl list-units --type=service --state=running
```

---

## نکات امنیتی

⚠️ **مهم:**
- رمز عبور ادمین را تغییر دهید (بلافاصله بعد از ورود)
- از رمز عبور قوی و منحصربه‌فرد استفاده کنید
- پنل ادمین فقط از `localhost` قابل دسترسی است — از SSH Tunnel استفاده کنید
- اطمینان حاصل کنید که Cloudflare یا reverse proxy شما `/admin` را در معرض عموم قرار نمی‌دهد
- جدول پایگاه داده `admins` حاوی هش‌های bcrypt است — هرگز رمز ساده‌ذخیره کنید

---

## مراحل بعدی

۱. **ایجاد کانفیگ VPN**: پنل ادمین → بخش "Configs" → ایجاد تنظیمات جدید
۲. **ایجاد اپلیکیشن اندروید**: پنل ادمین → بخش "Applications" → ایجاد اپ
۳. **مدیریت کاربران**: پنل ادمین → بخش "Users" → اضافه کردن کاربران جدید
۴. **نظارت فعالیت**: پنل ادمین → بخش "Monitoring" → مشاهده فعالیت‌های کاربران

---

## تماس و پشتیبانی

- مخزن GitHub: https://github.com/Lucifer-ir/vps
- برای گزارش مشکلات: Issues قسمت GitHub

---

**نصب موفق باشد! 🚀**
